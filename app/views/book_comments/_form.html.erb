<%= form_with model: [book, book_comment], class: "w-100" do |f| %>
  <div class="mb-2">
    <%= f.text_area :comment,
        rows: 5,
        class: "form-control w-100" %>
  </div>

  <div>
    <%= f.submit "送信" %>
  </div>
<% end %>